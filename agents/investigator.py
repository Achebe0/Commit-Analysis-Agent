import subprocess
import json


# pull the logs from the site( RAILWAY API ) -> GraphQL formatting

# if false fire or no issue then we don't run the subprocess and just return

# subprocess.run(["git","reset",,"--hard",f"{first_commit}",check=True)

#have an investigator agent analyze the possible reasons for the downtime.
def investigator_agent():
    # if false fire then we dont go further and just return

    pass
